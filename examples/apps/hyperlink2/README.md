# Hyperlink inline viewport demo

This example renders a hyperlink while drawing directly into the inline viewport. Press any key to exit the demo.

To run this demo:

```shell
cargo run -p hyperlink2
```
